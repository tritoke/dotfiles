[user]
  email = sam.leonard@codethink.co.uk
  name = Sam Leonard
	signingkey = 96850F0978CE78F0!

[alias]
  lol = log --graph --decorate --pretty=oneline --abbrev-commit
  loln = log --graph --decorate --pretty=oneline --abbrev-commit --name-status
  lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
  lolan = log --graph --decorate --pretty=oneline --abbrev-commit --all --name-status
  log1 = log --pretty=oneline --abbrev-commit
  lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
  difft = difftool
  dlog = "!f() { GIT_EXTERNAL_DIFF=difft git log -p --ext-diff; }; f"
  show-pick = "!git log --color=always --oneline | fzf --ansi --preview='git show --color=always $(echo {} | cut -d\\  -f1)' | awk '{print $1}' | xargs -r git show"

[color]
  branch = auto
  diff = auto
  interactive = auto
  status = auto

[pull]
	rebase = true

[init]
	defaultBranch = main

[commit]
	gpgsign = true
  
[diff]
  tool = difftastic

[difftool]
  prompt = false

[difftool "difftastic"]
  cmd = difft "$LOCAL" "$REMOTE"

[pager]
  difftool = true

[rerere]
	enabled = true

[includeIf "gitdir:~/.builds/"]
    path = .gitconfig-tritoke

[includeIf "gitdir:~/.dotfiles/"]
    path = .gitconfig-tritoke
