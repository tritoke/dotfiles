call sab#Iab('#d', '#define ')
call sab#Iab('#i', '#include <><Left>')
call sab#Iab('#I', '#include ""<Left>')
call sab#Iab('cin', 'std::cin >> ;<Left>')
call sab#Iab('cout', 'std::cout <<  << std::endl;<Esc>2F i')
call sab#Iab('cerr', 'std::cerr <<  << std::endl;<Esc>2F i')
call sab#Iab('for', 'for (;;) {<Enter>}<Esc>kf;i')
call sab#Iab('foreach', 'for (const auto & : ) {<Enter>}<Esc>$kF)i')
call sab#Iab('for_each', 'std::for_each(.begin(), <++>.end(), <++>);<Esc>3T(i')
call sab#Iab('while', 'while () {<Enter>}<Esc>kf)i')
call sab#Iab('switch', 'switch () {<Enter>}<Esc>kf)i')
call sab#Iab('if', 'if () {<Enter>}<Esc>kf)i')
call sab#Iab('else', 'else {<Enter>}<Esc>O')
call sab#Iab('elif', 'else if () {<Enter>}<Esc>kf)i')
call sab#Iab('main', 'int main() {<Enter>return 0;<Enter>}<Esc>kO')
call sab#Iab('argmain', 'int main(int argc, char ** argv) {<Enter>return 0;<Enter>}<Esc>kO')
call sab#Iab('envmain', 'int main(int argc, char ** argv, char ** envp) {<Enter>return 0;<Enter>}<Esc>kO')
